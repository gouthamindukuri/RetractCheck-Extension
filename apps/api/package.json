{
  "name": "@retractcheck/worker",
  "version": "0.2.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "build": "[ -f wrangler.toml ] && wrangler deploy --dry-run || echo 'Skipping build: wrangler.toml not found (copy wrangler.example.toml and configure)'",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "lint": "eslint src",
    "format": "prettier -w src",
    "clean": "rm -rf dist",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@retractcheck/doi": "workspace:*",
    "@retractcheck/types": "workspace:*",
    "csv-parse": "^6.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260120.0",
    "@eslint/js": "^9.39.2",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import": "^2.32.0",
    "globals": "^17.0.0",
    "happy-dom": "^20.3.4",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17",
    "wrangler": "^4.59.3"
  }
}
